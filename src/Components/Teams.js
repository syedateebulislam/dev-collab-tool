import React from 'react'

function Teams({TeamsProps}) {
    return (
        <div>

            <p>Username - {TeamsProps.Username}</p>
            <p>Status - {TeamsProps.Status}</p>
            <p>____________</p>

        </div>
    )
}

export default Teams
